function y = autoturn(x)% AUTOTURN	successively offset rows of an array%			or an array-nee-vector.  Useful for %			making space-time diagrams.%	AUTOTURN(X) does as just described.%	AUTOTURN(x) replicates x into an array, and%			does as just described. [r c] = size(x);if r == 1	x = repmat(x, c, 1);elseif c==1	x = repmat(x', r, 1);end	for i = 2:max([r c]);	x(i,:) = turn(x(i,:),-(i-1));endy=x;