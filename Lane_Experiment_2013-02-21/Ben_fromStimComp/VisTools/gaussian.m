function A = gaussian(n, s)% GAUSSIAN  makes a 1D gaussian vector.%	GAUSSIAN(N, S) makes a vector of length N containing a gaussian% 	with std. deviation s and unit area.%	GAUSSIAN(X, S), where X is a vector, computes the gaussian over%	the domain of Xif length(n) == 1	A=zeros(1, n);	x=-n/2:n/2-1;else	A=zeros(1, length(n));	x = n;endA=(1/(s*sqrt(2*pi))).*exp(-x.^2./(2*s^2));