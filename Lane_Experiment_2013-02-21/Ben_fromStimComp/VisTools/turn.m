function y=turn(x,n)%% TURN shifts all elements horizontally%% usage:%   Y=TURN(X, N)	shifts matrix or row vector elements by n .%   Elements that fall off one side appear on the other as though the data%   were on a cylinder.%   Positive N shifts rightward.  %%   See also: roll, shift, shear%% Lawrence K. Cormack% history:% 3/1/97    lkc Wrote it.% 7/4/02    lkc handles shifts larger than the # of columns% 7/4/02    lkc Cleaned up the comments.[height,last]=size(x);n = rem(n, last);y=ones(size(x));if(n>=0)	y(:,n+1:last)=x(:,1:last-n);	y(:,1:n)=x(:,last-n+1:last);else	n=abs(n);	y(:,1:last-n)=x(:,n+1:last);	y(:,last-n+1:last)=x(:,1:n);end