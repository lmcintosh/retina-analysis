function [v, m, b] = linefit(x, y);% LINEFIT fits a line%	[v, m, b] = linefit(x,y) gives the%		slope (m), y-intercept (b) of the best-fit%		line, and returns a vector of the values evaluated% 		at the elements of x.  If a plot already exists, the% 		fit is also plotted as a line. [c s] = polyfit(x, y, 1);m = c(1);b = c(2);[v delta] = polyval(c, x, s);% if the data are already plotted, plot the fit as a lineif(~isempty(findobj('type', 'axes'))),	hold on;	plot(x,v,'-');end%delta