function y=roll(x, n)%% ROLL shifts all elements vertically%% usage:% Y=ROLL(X, N)	rolls a matrix or column vector by n elements.% Elements that roll off one end roll onto the other as though the data% were on a cylinder.% Positive N rolls downward.%%   See also: turn, shift, shear%% Lawrence K. Cormack% history:% 3/1/97    lkc Wrote it.% 7/4/02    lkc handles shifts larger than the # of columns% 7/4/02    lkc Cleaned up the comments.[last,width]=size(x);n = rem(n, last);y=ones(size(x));if(n>=0)	y(n+1:last,:)=x(1:last-n,:);	y(1:n,:)=x(last-n+1:last,:);else	n=abs(n);	y(1:last-n,:)=x(n+1:last,:);	y(last-n+1:last,:)=x(1:n,:);end