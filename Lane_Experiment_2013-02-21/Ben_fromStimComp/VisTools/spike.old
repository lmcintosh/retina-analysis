function s=spike(alpha, r, c)% SPIKE - 	creates a spike image, an array in which the%			value is inversely proportional to the distance%			from the center.  Useful for 1/f noise (use s.^2).% 11/12/2000 - updated for any rxc size% 11/12/2000 - added odd/even dependent shift to compensate for the%			location of the DC after fftshift()% 3/31/2002 - added the exponent, alpha, to this function (where it belongs)%           rather than dealing with it in oneoverf()%% See also:  oneoverf(), spike3d(), oneoverf3d()% lkc if nargin == 2 c=r; ends = ones(r, c); [y x] = find(s);if isodd(max(x))	x = x - (max(x)/2 + 0.5);else	x = x - (max(x)/2 + 1);endif isodd(max(y))	y = y - (max(y)/2 + 0.5);else	y = y - (max(y)/2 + 1);enddists = sqrt(x.^2 + y.^2);dists = reshape(dists, r, c);smallest = mmin(dists);dists = dists + (1-smallest);s = s./dists.^alpha;