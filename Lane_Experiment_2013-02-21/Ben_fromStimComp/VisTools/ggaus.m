function A = ggaus(n, s1, s2, o)%% GGAUS  makes a 2D gaussian array.%% usage:%	GGAUS(N, S) makes a circulary symmetric gaussian.%	GGAUS(N, S1, S2) makes a gaussian with vertical stdev S1.%	GGAUS(N, S1, S2, R) makes an elliptical gaussian rotated CCW by %		R degrees (requires the image processing toolbox).%     % see also: ddog, ggab% % Lawrence K. Cormack% history:% 11/16/00   lkc Wrote it.% 7/3/02   lkc Cleaned up the comments.if nargin < 3, s2 = s1; endA=zeros(n);x=-n/2:n/2-1;[X,Y]=meshgrid(x);A=1./exp(Y.^2./(2*s1^2)) .* 1./exp(X.^2./(2*s2^2));if nargin == 4,	A = imrotate(A, o, 'crop');end