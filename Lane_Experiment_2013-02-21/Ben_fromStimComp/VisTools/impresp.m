function f = impresp(tau,na,nb);% impresp(tau,ordera,orderb)% mono- or biphasic temporal impulse response function% tau is time constant (e.g. 6)% ordera is positive component order (e.g. 2)% orderb [optional] is negative component order (e.g. 10)% 2.24.01 LKC (sbs)% tmpresp - make a bi-phasic temporal impulse response% tau = 6; % time constant% na = 2; % order of filter% nb = 10;t=0:128;afact = prod(1:na-1);a = (t./tau).^(na-1) .* exp(-t./tau) ./ (tau*afact);f = a;if nargin > 2	bfact = prod(1:nb-1);	b = (t./tau).^(nb-1) .* exp(-t./tau) ./ (tau*bfact);	f = f - b;end